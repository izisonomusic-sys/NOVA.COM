import {Injectable}from'@nestjs/common';import {Cron}from'@nestjs/schedule';import {PrismaService}from'./prisma.service';import {PlatformEventsService}from'./events/events.service';import {PLATFORM_EVENTS}from'./events/platform-events';
@Injectable()export class InvestmentSettlementService{constructor(private p:PrismaService, private events:PlatformEventsService){}@Cron('*/5 * * * *')async settle(){try{const result:any=await this.p.$queryRaw`select public.settle_matured_investments() as processed`;const processed=Number(result?.[0]?.processed||0);if(processed>0)this.events.emit(PLATFORM_EVENTS.INVESTMENT_MATURED,{metadata:{processed}})}catch(e){console.error('settlement',e)}}}
