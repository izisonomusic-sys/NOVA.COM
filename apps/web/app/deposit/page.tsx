'use client';
import Link from 'next/link';
import {useEffect,useMemo,useState} from 'react';
import {api,money,go} from '../../lib/api';
const methods=[{id:'tmoney',name:'TMoney',hint:'Togocel Money',mark:'T'},{id:'moov',name:'Moov Money',hint:'Flooz / Moov',mark:'M'},{id:'mixx',name:'Mixx',hint:'Mixx by Yas',mark:'X'}];
const amounts=[1000,2000,3000,5000,10000,20000,50000,100000];
export default function DepositPage(){const [wallet,setWallet]=useState<any>({});const [amount,setAmount]=useState(1000);const [custom,setCustom]=useState('');const [method,setMethod]=useState('tmoney');const [loading,setLoading]=useState(false);const [msg,setMsg]=useState('');const value=Number(custom||amount||0);const selected=useMemo(()=>methods.find(x=>x.id===method),[method]);useEffect(()=>{api('/wallet').then(setWallet).catch(()=>go('/login'))},[]);async function submit(){setMsg('');if(!Number.isFinite(value)||value<1000){setMsg('Le dépôt minimum est de 1 000 XOF.');return;}setLoading(true);try{const r=await api('/payments/deposits',{method:'POST',body:JSON.stringify({amount:value,method})});setMsg('Ordre de dépôt créé. Redirection vers le paiement sécurisé…');if(r.checkoutUrl&&r.checkoutUrl.startsWith('http')&&!r.checkoutUrl.includes('/payment/mock'))window.location.href=r.checkoutUrl;else if(process.env.NEXT_PUBLIC_PAYDUNYA_MODE==='mock')setMsg('Ordre créé en mode test. Vous pouvez confirmer depuis le portefeuille.')}catch(e:any){setMsg(e.message||'Impossible de créer le dépôt.')}finally{setLoading(false)}}return <main className="money-page"><div className="container money-shell"><div className="money-top"><Link href="/dashboard" className="money-back">←</Link><div><h1>Dépôt</h1><p>Ajoutez des fonds à votre compte pour commencer à investir.</p></div></div><section className="money-card money-balance"><div><span>Solde du compte</span><strong>{money(wallet?.balance)}</strong></div><div className="money-currency">🇹🇬 XOF (CFA)</div><div className="money-bonus">🎁 <b>Bonus</b> <span>0 XOF</span></div></section><section className="money-card"><h2>Méthode de paiement</h2><div className="method-grid">{methods.map(m=><button type="button" onClick={()=>setMethod(m.id)} className={'method-choice '+(method===m.id?'selected':'')} key={m.id}><span className={'method-mark '+m.id}>{m.mark}</span><span><b>{m.name}</b><small>{m.hint}</small></span></button>)}</div></section><section className="money-card"><h2>Montant du dépôt</h2><div className="amount-grid">{amounts.map(v=><button type="button" key={v} onClick={()=>{setAmount(v);setCustom('')}} className={'amount-choice '+(!custom&&amount===v?'selected':'')}>{money(v)}</button>)}</div><label className="money-label">Montant personnalisé</label><div className="money-input-wrap"><input className="money-input" inputMode="numeric" value={custom} onChange={e=>setCustom(e.target.value.replace(/\D/g,''))} placeholder="Entrez le montant"/><span>XOF</span></div></section><section className="money-summary"><div><span>Opérateur sélectionné</span><b>{selected?.name}</b></div><div><span>Montant à payer</span><b>{money(value)}</b></div><div><span>Frais</span><b>0 XOF</b></div></section><button className="money-submit" onClick={submit} disabled={loading}>{loading?'Création du paiement…':'🔒 Déposer maintenant'}</button><p className="money-note">En continuant, vous serez dirigé vers le parcours de paiement sécurisé de notre prestataire.</p>{msg&&<p className={msg.includes('créé')||msg.includes('Redirection')?'success':'error'}>{msg}</p>}</div></main>}
